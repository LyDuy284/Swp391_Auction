import 'package:flutter/material.dart';

class AuctionAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Auction Admin'),
      ),
      body: ListView(
        children: [
          // Your auction items widgets go here
        ],
      ),
    );
  }
}
